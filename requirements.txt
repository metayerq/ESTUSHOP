flask
requests
# Base de fuseaux horaires pour zoneinfo. L'image Vercel embarque normalement
# /usr/share/zoneinfo, mais on ne peut pas le vérifier depuis ici : sans base,
# ZoneInfo("Europe/Lisbon") lèverait au premier appel. Roue pure-python, ~350 ko.
tzdata
